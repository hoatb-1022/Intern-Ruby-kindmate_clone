$("#donation-render").html("<%= j render @donations_paged %>");
$("#donation-paginator").html("<%= j paginate @donations_paged, remote: true %>");

$("#comment-render").html("<%= j render @comments %>");
$("#comment-paginator").html("<%= j paginate @comments, remote: true %>");
